# AKIMBOT

## Installation

1. Rendre le script executable:
	`chmod +x install.sh`
2. Lancer l'installation:
	`./install.sh`

Le script cree un environnement virtuel `.venv` puis installe tous les paquets de `requirements.txt`.
